# Tells Grafana where to find dashboard JSON files to auto-load.
apiVersion: 1

providers:
  - name: "TrialTerminator"
    folder: "TrialTerminator"
    type: file
    options:
      path: /var/lib/grafana/dashboards
